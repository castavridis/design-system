# Layout sync 20260806T204301Z

Direction: Figma -> code. 0 entries.

| outcome | count |
| --- | --- |


A layout change moves every card. Re-render with `pnpm og:demo` afterwards,
then `pnpm figma:og` to send the new artwork back.

No files were changed. Re-run with `--apply` to write them.
